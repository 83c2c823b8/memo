sudo apt install -y gnome-tweaks, vim, git, gh, curl, ibus-mozc, mozc-utils-gui
gh auth login
git clone https://github.com/83c2c823b8/ubuntu_environments
curl -fsSL https://deno.land/install.sh | sh
sudo snap install discord vlc
sudo apt install tex-livefull
